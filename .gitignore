__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
build/
dist/

# All per-role run output (manifest, resumes, scores, ranked CSVs,
# ranked_resumes/) lives under results/ — see storage/layout.py.
/results/

# Safety net: never commit run artifacts or candidate PII, wherever they land.
# (The examples/ fixtures use distinct names — sample-candidates.csv,
# sample-resumes/ — so they stay tracked.)
**/manifest.sqlite
**/resumes/
**/scores/
**/ranked_resumes/
ranked.csv
ranked_full.csv
candidates.csv

# Local config / secrets
.env
.env.local

# Editor
.vscode/
.idea/
.DS_Store

# Local working notes (never commit)
notes.txt
notes.md
NOTES.md
scratch/

# Internal design docs / historical specs (stay local)
docs/
